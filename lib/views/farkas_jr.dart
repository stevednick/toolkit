import 'package:flutter/material.dart';

class FarkasJr extends StatelessWidget {
  const FarkasJr({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
